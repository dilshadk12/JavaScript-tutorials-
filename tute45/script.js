first.className="text-dark red";  // on execution it will turn background red and color black

first.classList.remove("red");  // remove individual class
first.classList.add("red");  // add individual class

first.classList.toggle("red"); // if red is present then it will remove it 
                              // if red not present then it will add it.

first.classList.contains("red");  // check whether class present or not
          
